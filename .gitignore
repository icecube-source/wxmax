# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtualenvs
.venv/
venv/
env/

# Local R&D data/caches + the live panel's runtime state + dashboard.
# The panel now lives ONLY on the local machine (no GitHub Pages / cloud cron),
# so its state and rendered dashboard are untracked.
data/
data/*.parquet
data/*.duckdb
panel_data/
docs/

# Secrets — never commit API keys
.env
*.key
secrets.*

# OS / editor
.DS_Store
.vscode/
.idea/
