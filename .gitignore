# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtualenvs
.venv/
venv/
env/

# Local R&D data / caches (forecasts, obs, feature store).
# NOTE: panel_data/ (committed panel state) and docs/ (dashboard) are tracked.
data/
data/*.parquet
data/*.duckdb

# Secrets — never commit API keys
.env
*.key
secrets.*

# OS / editor
.DS_Store
.vscode/
.idea/
